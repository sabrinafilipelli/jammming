import { Button, Box } from "@mui/material";

const SearchButton = () => {
  return (
    <>
      <Button
        sx={{ width: "50%", height: "3rem", margin: "0 auto" }}
        variant="contained"
      >
        Search
      </Button>
    </>
  );
};

export default SearchButton;
